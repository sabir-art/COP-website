# Current Task

## Status

No active Webflow change is currently approved in this repository memory.

The current cleanup task is repository-memory cleanup only.

## Active instruction

Keep `sabir-art/COP-website` clean and practical for Claude, Cursor, and future agents.

Use branch `cursor/setup-dev-environment-ee62` as the single source of truth.

Do not create new branches, pull requests, or repositories without Abdellah's explicit approval.

Do not modify the live Webflow website unless Abdellah asks for a specific Webflow change.

## Current repository cleanup goal

Make the memory files useful without biasing future agents.

That means:

- Keep only current active work in this file.
- Keep confirmed facts in `VALIDATED_CONTEXT.md`.
- Keep unresolved questions in `QUESTIONS.md`.
- Keep reproducible current limitations in `FAILED_ATTEMPTS.md`.
- Keep long historical details out of active memory files.

## Notes for next agent

Before doing any Webflow work, inspect the current live structure first. Old notes may be outdated.
