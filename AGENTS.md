# AGENTS.md

## Cursor Cloud specific instructions

This repository ("COP Website") is currently an empty scaffold with only a `README.md`. There is no application code, no package manager configuration, no tests, and no services defined yet.

### Current state

- **No dependencies** to install (no `package.json`, `requirements.txt`, `go.mod`, etc.)
- **No build or lint commands** available
- **No tests** to run
- **No services** to start

### For future agents

Once application code is added, update this file with:
- How to install dependencies
- How to run the dev server
- How to run lint and tests
- Any non-obvious environment requirements or gotchas
